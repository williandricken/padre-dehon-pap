<%= form_for(@behavior) do |f| %>
  <% if @behavior.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@behavior.errors.count, "error") %> prohibited this behavior from being saved:</h2>

      <ul>
      <% @behavior.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label "Descrição" %><br>
    <%= f.text_field :description %>
  </div>
  <div class="actions">
    <%= f.submit "Adicionar novo comportamento" %>
  </div>
<% end %>
